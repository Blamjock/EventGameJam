using UnityEngine;
using System.Collections;

public class ColorChanger : MonoBehaviour {
	/*public Material materialBlue;
	public Material materialGreen;
	public myColors mycolor;
	// Use this for initialization
	void Start () {
		switch (mycolor) {
		case myColors.Blue:
			this.gameObject.GetComponent<Renderer> ().material = materialBlue;
			break;
		case myColors.Green:
			this.gameObject.GetComponent<Renderer> ().material = materialGreen;
			break;
		}
	}
	
	// Update is called once per frame
	void Update () {

	}

	void changeColor(myColors newcolor)
	{
		mycolor = newcolor;
		switch (mycolor) {
		case myColors.Blue:
			this.gameObject.GetComponent<Renderer> ().material = materialBlue;
			break;
		case myColors.Green:
			this.gameObject.GetComponent<Renderer> ().material = materialGreen;
			break;
		}
	}*/
}
